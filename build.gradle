plugins {
	// see https://fabricmc.net/develop/ for new versions
	id 'fabric-loom' version '1.16-SNAPSHOT' apply false
	// see https://projects.neoforged.net/neoforged/moddevgradle for new versions
	id 'net.neoforged.moddev' version '2.0.141' apply false
}

tasks.register('buildTrimmed') {
	group = 'build'
	description = 'Builds trimmed Fabric and NeoForge test jars into build/trimmed.'

	dependsOn(':fabric:buildTrimmed', ':neoforge:buildTrimmed')
}
